<%= form_for(@deadline) do |f| %>
  <% if @deadline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deadline.errors.count, "error") %> prohibited this deadline from being saved:</h2>

      <ul>
      <% @deadline.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="student_table">

  <div class="field">
    <%= f.label :semester %><br />
    <%= f.text_field :semester %>
  </div>
  <div class="field">
    <%= f.label :start_of_registration %><br />
    <%= f.date_select :start_of_registration %>
  </div>
  <div class="field">
    <%= f.label :start_of_classes %><br />
    <%= f.date_select :start_of_classes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

</div> 
